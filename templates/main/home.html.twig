{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Accueil
{% endblock %}

{% block body %}
    <h2>Evènements à venir</h2>
    <table>
        <tr>A</tr>
        <tr>A</tr>

        <td>{{ ev1.nom }}</td>
        <td>b</td>
    </table>

    {# ----Tableau des évènements----  #}
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Lundi</th>
                <th scope="col">Mardi</th>
                <th scope="col">Mercredi</th>
                <th scope="col">Jeudi</th>
                <th scope="col">Vendredi</th>
                <th scope="col">Samedi</th>
                <th scope="col">Dimanche</th>
            </tr>
        </thead>
        <tbody>
            {% set nbDay = 0 %}
            {% for i in 0..5 %}
            <tr>
                {% for i in 0..6 %}
                {% set nbDay = nbDay + 1 %}
                <td>
                    {{ nbDay }}
                </td>
                {% endfor %}
            </tr>
            {% endfor %}
        </tbody>
    </table>

    {# ----Liste des évènements passés---- #}
    {% set article = {
        titre: "Concours salle 18 et 19 décembre 2021",
        photo: "photo1.jpg",
        desc: "abla bla bla"
    } %}

    <h2>{{ article.titre | upper }}</h2>

    {% for key, val in article %}
        <div>{{ key }} : {{ val }} </div>
    {% endfor %}

{% endblock %}

